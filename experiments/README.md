# experiments

One config file per experiment, reproducible from config + seed.

A config names everything that changes an outcome: which personas, which items,
which model, which strategy, the seed. Results go to `results/`, and the entry in
`results/PROJECT_LOG.md` links the config to its result file.
