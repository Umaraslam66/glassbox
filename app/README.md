# app

Minimal local dashboard. Reads files from `results/` and `data/public/` and runs on
localhost. No auth, no deployment, no styling effort.

Pages are added as each stage produces the data they show (see PRD section 8):
population, recovery, person encoder, calibration, interviewer.
