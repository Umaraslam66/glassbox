[pytest]
testpaths = tests
# Repo root on the import path: mobility modules import as mobility.src.*;
# glassbox/ beside it so parent modules stay importable unchanged as src.*
# (PRD_MOBILITY.md section 2).
pythonpath = .. ../glassbox
