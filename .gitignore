# Data: personas, transcripts, truth store. Never committed.
data/

# Secrets
.env
.env.*

# Personal ops docs (contain account details)
LEONARDO_HOWTO.md
LEONARDO_REFERENCE.md

# Fitted arrays in results/ (~23 MB): every one is regenerated by a command in
# README.md. The JSON report and the PNG beside each one ARE committed -- those
# are what the dashboard and the writeup read.
results/*.npz

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/
*.egg-info/

# macOS
.DS_Store
